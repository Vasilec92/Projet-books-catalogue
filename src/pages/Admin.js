import React from "react";

import TableBook from "../components/TableBook.js";
const Admin = ({ data }) => {
  return (
    <div>
      <TableBook data={data} />
    </div>
  );
};

export default Admin;
